# 1) Footer의 프로젝트 문의 버튼 → 브랜드 버튼
apply_patch <<'PATCH'
*** Begin Patch
*** Update File: client/src/components/Layout/Footer.tsx
@@
-          <a href="/inquiry" className="...">프로젝트 문의</a>
+          <a href="/inquiry" className="btn-brand">프로젝트 문의</a>
*** End Patch
PATCH

# 2) Blog 리스트/상세 페이지 버튼 → 브랜드 버튼, 태그/카테고리 → 배지 브랜드
apply_patch <<'PATCH'
*** Begin Patch
*** Update File: client/src/pages/Blog/BlogList.tsx
@@
-            <a href={\`/blog/\${post.slug}\`} className="...">자세히 보기</a>
+            <a href={\`/blog/\${post.slug}\`} className="btn-brand">자세히 보기</a>
*** End Patch
PATCH

apply_patch <<'PATCH'
*** Begin Patch
*** Update File: client/src/pages/Blog/BlogItem.tsx
@@
-        <div className="flex flex-wrap gap-2">
-          {post.tags?.map((t:string)=>(<span key={t} className="...">{t}</span>))}
-        </div>
+        <div className="flex flex-wrap gap-2">
+          {post.tags?.map((t:string)=>(<span key={t} className="badge-brand">{t}</span>))}
+        </div>
*** End Patch
PATCH

# 3) Portfolio 리스트/상세 CTA 버튼/배지 → 브랜드 스타일
apply_patch <<'PATCH'
*** Begin Patch
*** Update File: client/src/pages/Portfolio/PortfolioList.tsx
@@
-                <a href={\`/portfolio/\${item.slug}\`} className="...">자세히 보기</a>
+                <a href={\`/portfolio/\${item.slug}\`} className="btn-brand">자세히 보기</a>
*** End Patch
PATCH

apply_patch <<'PATCH'
*** Begin Patch
*** Update File: client/src/pages/Portfolio/PortfolioItem.tsx
@@
-        <div className="flex flex-wrap gap-2">
-          {data?.tags?.map((t:string)=>(<span key={t} className="...">{t}</span>))}
-        </div>
+        <div className="flex flex-wrap gap-2">
+          {data?.tags?.map((t:string)=>(<span key={t} className="badge-brand">{t}</span>))}
+        </div>
*** End Patch
PATCH

# 4) 빌드 & 재시작
bash -lc 'npm run build --silent || true'
bash -lc 'pkill -f "node" || true; npm start --silent & sleep 2'

# 5) 검증: btn-brand / badge-brand 적용 확인
bash -lc '
echo "[Footer check]" ; grep -n "btn-brand" client/src/components/Layout/Footer.tsx || true
echo "[Blog list check]" ; grep -n "btn-brand" client/src/pages/Blog/BlogList.tsx || true
echo "[Blog tags check]" ; grep -n "badge-brand" client/src/pages/Blog/BlogItem.tsx || true
echo "[Portfolio list check]" ; grep -n "btn-brand" client/src/pages/Portfolio/PortfolioList.tsx || true
echo "[Portfolio tags check]" ; grep -n "badge-brand" client/src/pages/Portfolio/PortfolioItem.tsx || true
'